# Аудит кода (кратко)

## Что уже исправлено

1. Секреты и локальные окружения:
   - `.gitignore` усилен: теперь игнорируются `.env` и все `.env.*` (кроме `.env.example`).
   - Добавлен шаблон `.env` с плейсхолдерами.
2. Читаемость и поддерживаемость:
   - `app/api/auth/login/route.ts`: вынесены константы ошибок и проверка пароля в отдельную функцию.
   - `src/bot/aiParser.ts`: убрано дублирование инициализации Yandex/OpenAI-клиента, используется общий модуль `lib/yandex.ts`.
   - `src/bot/env.ts`: загрузка окружения стала более явной (`.env` + `.env.local` с приоритетом локального файла).
3. Инициализирован Git-репозиторий (`git init`).

## Критичные риски

1. Пароли сравниваются в открытом виде (`user.password === inputPassword`), без хеширования.
2. Не видно защиты от brute-force атак (rate-limit на логин).
3. Нет централизованной ESLint-конфигурации, из-за чего сложнее удерживать единый стиль и читаемость на длинной дистанции.

## Рекомендованный следующий шаг

1. Перейти на хеширование паролей (`bcrypt` или `argon2`) с миграцией существующих пользователей.
2. Добавить rate-limit на `/api/auth/login`.
3. Добавить рабочую конфигурацию ESLint + Prettier и включить проверку в CI.
